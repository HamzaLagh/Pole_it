apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: php-fpm-claim0
  name: php-fpm-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
